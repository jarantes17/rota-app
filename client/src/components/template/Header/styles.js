import styled from "styled-components"

export const IconContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`

export const Container = styled.header`
  grid-area: header;

  padding: 0px 15px;
  overflow: hidden;
  white-space: nowrap;
  box-shadow: 0 40px 23px -20px #e6e6f0, 0 40px 49px -20px #e6e6f0;
  background-color: #fff;

  h1 {
    font-size: 1.8em;
  }
`
